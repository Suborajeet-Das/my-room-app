# Database connection strings
# Secret keys for authentication
# API keys (if needed)
# Why?
# Keeps sensitive data secure and out of the code.
# Prevents hardcoding values directly into your JavaScript files.



MONGO_URI=<your-mongodb-connection-string>
JWT_SECRET=<your-jwt-secret>
